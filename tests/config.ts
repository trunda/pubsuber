import "./init";
import {expect} from "chai";
import {config} from "../src/util";

describe("options.spec", () => {
    // it("uses default options", () => {
    //     expect(options).to.deep.eq(require("../src/util/default-options.json"));
    // });

    // it("merges options from file in working directory", () => {
    //
    // });
});